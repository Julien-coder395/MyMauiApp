<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MyMauiApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MyMauiApp"
    xmlns:views="clr-namespace:MyMauiApp.Views"
    Shell.FlyoutBehavior="Disabled">

    <!--Commenter/Décommenter le code ci-dessous pour masquer/afficher la MainPage.-->
    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />-->

    <!--Commenter/Décommenter le code ci-dessous pour masquer/afficher les vues se trouvant dans le dossier "Views". -->
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate views:LoginPage}" 
        Route="MainPage" />
    <!--Il faut aussi changer le numéro MainPage2, MainPage3-->

</Shell>
